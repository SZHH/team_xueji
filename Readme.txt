
    authority:用户恶意访问时跳的页(权限不足但还通过域名强行访问)；
    selectcompare:选择想要对比的点名记录；
    compare:进行对比；
    courses_list:进成绩页时显示可选班级的课程；
    rollcall_add:进行点名(点名即增加记录，所以这么命名)；
    rollcalltime:选择想要查询的点名记录(点时间进下页，所以这么命名)；
    rollcall:显示点名记录(查)；
    validate:登陆时进行判断的页(代替之前的dispose，其译为“验证”)；
    register:译为注册。